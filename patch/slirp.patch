diff --git a/src/slirp.c b/src/slirp.c
index f26c987..13f9f12 100644
--- a/src/slirp.c
+++ b/src/slirp.c
@@ -366,6 +366,21 @@ int get_dns6_addr(struct in6_addr *pdns6_addr, uint32_t *scope_id)
 #define RESOLV_CONF_PATH "/etc/resolv.conf"
 #endif
 
+#if defined(__ANDROID__)
+#define ANDROID_DNS4 "8.8.8.8"
+#define ANDROID_DNS6 "2001:4860:4860::8888"
+
+static const char *get_resolv_conf_path(void)
+{
+    return "/sdcard/qemu/resolv.conf";
+}
+#else
+static const char *get_resolv_conf_path(void)
+{
+    return RESOLV_CONF_PATH;
+}
+#endif
+
 static int get_dns_addr_cached(void *pdns_addr, void *cached_addr,
                                socklen_t addrlen, struct stat *cached_stat,
                                unsigned *cached_time)
@@ -376,7 +391,23 @@ static int get_dns_addr_cached(void *pdns_addr, void *cached_addr,
         return 0;
     }
     old_stat = *cached_stat;
-    if (stat(RESOLV_CONF_PATH, cached_stat) != 0) {
+    if (stat(get_resolv_conf_path(), cached_stat) != 0) {
+#if defined(__ANDROID__)
+        if (addrlen == sizeof(struct in_addr)) {
+            struct in_addr *addr = (struct in_addr *)cached_addr;
+            if (addr->s_addr != 0) {
+                *cached_time = curtime;
+                memcpy(pdns_addr, cached_addr, addrlen);
+                return 0;
+            }
+        } else {
+            if (!in6_zero((struct in6_addr *)cached_addr)) {
+                *cached_time = curtime;
+                memcpy(pdns_addr, cached_addr, addrlen);
+                return 0;
+            }
+        }
+#endif
         return -1;
     }
     if (cached_stat->st_dev == old_stat.st_dev &&
@@ -439,13 +470,27 @@ static int get_dns_addr_resolv_conf(int af, void *pdns_addr, void *cached_addr,
     char buff[512];
     char buff2[257];
     FILE *f;
     int found = 0;
     unsigned if_index;
     unsigned nameservers = 0;
 
-    f = fopen(RESOLV_CONF_PATH, "r");
-    if (!f)
-        return -1;
+    f = fopen(get_resolv_conf_path(), "r");
+    if (!f) {
+#if defined(__ANDROID__)
+        if (af == AF_INET) {
+            found += try_and_setdns_server(af, found, 0, ANDROID_DNS4,
+                            pdns_addr, cached_addr, addrlen, scope_id,
+                            cached_scope_id, cached_time);
+        } else {
+            found += try_and_setdns_server(af, found, 0, ANDROID_DNS6,
+                            pdns_addr, cached_addr, addrlen, scope_id,
+                            cached_scope_id, cached_time);
+        }
+        return found ? 0 : -1;
+#else
+        return -1;
+#endif
+    }
 
     DEBUG_MISC("IP address of your DNS(s):");
     while (fgets(buff, 512, f) != NULL) {
